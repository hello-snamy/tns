---
layout: base
title: తాజా తెలుగు వార్తలు
description: తాజా తెలుగు వార్తలు, బ్రేకింగ్ న్యూస్, రాష్ట్ర వార్తలు, జాతీయ, అంతర్జాతీయ వార్తలు
permalink: /
---

<!-- Hero Section -->
<section class="hero-section bg-primary text-white py-5">
    <div class="container">
        <div class="row align-items-center">
            <div class="col-lg-8">
                <h1 class="display-4 fw-bold mb-3">తాజా తెలుగు వార్తలు</h1>
                <p class="lead mb-4">బ్రేకింగ్ న్యూస్, రాష్ట్ర వార్తలు, జాతీయ, అంతర్జాతీయ వార్తలు</p>
                <div class="hero-buttons">
                    <a href="#featured-news" class="btn btn-light btn-lg me-3">
                        <i class="bi bi-newspaper me-2"></i>తాజా వార్తలు
                    </a>
                    <a href="/admin/" class="btn btn-outline-light btn-lg">
                        <i class="bi bi-pencil-square me-2"></i>వార్తలు సేర్చండి
                    </a>
                </div>
            </div>
            <div class="col-lg-4 text-center">
                <div class="hero-icon">
                    <i class="bi bi-megaphone display-1 text-warning"></i>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- Breaking News Alert -->
<div class="alert alert-danger alert-dismissible fade show rounded-0 mb-0" role="alert">
    <div class="container">
        <div class="row align-items-center">
            <div class="col-auto">
                <span class="badge bg-dark me-2">బ్రేకింగ్</span>
            </div>
            <div class="col">
                <marquee behavior="scroll" direction="left" class="breaking-marquee">
                    <span class="me-4">🔴 ఆంధ్రప్రదేశ్ బడ్జెట్ సదస్సు ఫిబ్రవరి 5న</span>
                    <span class="me-4">🔴 తెలంగాణలో కొత్త ఉద్యోగాల ప్రకటన</span>
                    <span class="me-4">🔴 భారత్ vs ఇంగ్లండ్ టెస్ట్ సిరీస్ ప్రారంభం</span>
                </marquee>
            </div>
            <div class="col-auto">
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        </div>
    </div>
</div>

<!-- Ad Unit -->
<div class="container my-4">
    <div class="text-center text-muted small mb-2">ప్రకటన</div>
    <div class="ad-unit bg-light border rounded d-flex align-items-center justify-content-center" style="height: 90px;">
        Leaderboard Ad (728x90)
    </div>
</div>

<!-- Featured News -->
<section id="featured-news" class="py-5">
    <div class="container">
        <div class="row mb-4">
            <div class="col">
                <h2 class="fw-bold border-bottom pb-2">ప్రధాన వార్తలు</h2>
            </div>
            <div class="col-auto">
                <a href="/news/" class="btn btn-outline-primary btn-sm">అన్నీ చూడండి</a>
            </div>
        </div>
        
        <div class="row g-4">
            {% for post in collections.news | slice(0, 3) %}
            <div class="col-lg-4 col-md-6">
                <div class="card news-card h-100 shadow-sm border-0">
                    {% if post.data.image %}
                    <img src="{{ post.data.image }}" class="card-img-top" alt="{{ post.data.title }}" style="height: 200px; object-fit: cover;">
                    {% endif %}
                    <div class="card-body d-flex flex-column">
                        <h5 class="card-title">
                            <a href="{{ post.url }}" class="text-decoration-none text-dark">{{ post.data.title }}</a>
                        </h5>
                        <p class="card-text text-muted flex-grow-1">{{ post.data.description | truncate(120) }}</p>
                        <div class="d-flex justify-content-between align-items-center">
                            <small class="text-muted">
                                <i class="bi bi-clock me-1"></i>
                                <time datetime="{{ post.date | isoDate }}">{{ post.date | readableDate }}</time>
                            </small>
                            {% if post.data.categories %}
                            <span class="badge bg-primary">{{ post.data.categories[0] }}</span>
                            {% endif %}
                        </div>
                        <a href="{{ post.url }}" class="btn btn-primary btn-sm mt-3">చదవండి</a>
                    </div>
                </div>
            </div>
            {% else %}
            <div class="col-12 text-center py-5">
                <div class="empty-state">
                    <i class="bi bi-newspaper display-1 text-muted mb-3"></i>
                    <h3 class="text-muted">ఇంకా వార్తలు లేవు</h3>
                    <p class="text-muted">మొదటి వార్తను సేర్చడానికి అడ్మిన్ పేజీకి వెళ్లండి</p>
                    <a href="/admin/" class="btn btn-primary">వార్తలు సేర్చండి</a>
                </div>
            </div>
            {% endfor %}
        </div>
    </div>
</section>

<!-- Newsletter Section -->
<section class="newsletter-section bg-light py-5">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-lg-6 text-center">
                <h2 class="fw-bold mb-3">తాజా వార్తలను మిస్ చేయకండి</h2>
                <p class="text-muted mb-4">రోజువారీ తెలుగు వార్తలు మీ ఇన్బాక్స్‌లో పొందండి</p>
                
                <form class="newsletter-form">
                    <div class="input-group input-group-lg">
                        <input type="email" class="form-control" placeholder="మీ ఈమెయిల్ చిరునామా" required>
                        <button class="btn btn-primary" type="submit">
                            <i class="bi bi-send me-2"></i>చందా పొందండి
                        </button>
                    </div>
                </form>
                
                <p class="text-muted small mt-3">
                    మేము మీ ప్రైవసీని గౌరవిస్తాము. ఎప్పుడైనా అన్సబ్స్క్రైబ్ చేసుకోవచ్చు
                </p>
            </div>
        </div>
    </div>
</section>

<!-- Categories Section -->
<section class="categories-section py-5">
    <div class="container">
        <div class="row mb-4">
            <div class="col">
                <h2 class="fw-bold text-center mb-3">వార్తా వర్గాలు</h2>
                <p class="text-muted text-center">మీకు అవసరమైన వర్గం ఎంచుకోండి</p>
            </div>
        </div>
        
        <div class="row g-4">
            <div class="col-lg-3 col-md-6">
                <a href="/categories/state/" class="card category-card text-decoration-none h-100 border-0 shadow-sm">
                    <div class="card-body text-center p-4">
                        <div class="category-icon mb-3">
                            <i class="bi bi-building display-4 text-primary"></i>
                        </div>
                        <h5 class="card-title">రాష్ట్ర వార్తలు</h5>
                        <p class="card-text text-muted small">ఆంధ్రప్రదేశ్ & తెలంగాణ తాజా వార్తలు</p>
                        <span class="badge bg-light text-dark">25 వార్తలు</span>
                    </div>
                </a>
            </div>
            
            <div class="col-lg-3 col-md-6">
                <a href="/categories/national/" class="card category-card text-decoration-none h-100 border-0 shadow-sm">
                    <div class="card-body text-center p-4">
                        <div class="category-icon mb-3">
                            <i class="bi bi-flag display-4 text-success"></i>
                        </div>
                        <h5 class="card-title">జాతీయ వార్తలు</h5>
                        <p class="card-text text-muted small">దేశం నలుమూలల నుండి వార్తలు</p>
                        <span class="badge bg-light text-dark">18 వార్తలు</span>
                    </div>
                </a>
            </div>
            
            <div class="col-lg-3 col-md-6">
                <a href="/categories/international/" class="card category-card text-decoration-none h-100 border-0 shadow-sm">
                    <div class="card-body text-center p-4">
                        <div class="category-icon mb-3">
                            <i class="bi bi-globe display-4 text-info"></i>
                        </div>
                        <h5 class="card-title">అంతర్జాతీయ వార్తలు</h5>
                        <p class="card-text text-muted small">ప్రపంచ వార్తలు, అంతర్జాతీయ సంబంధాలు</p>
                        <span class="badge bg-light text-dark">12 వార్తలు</span>
                    </div>
                </a>
            </div>
            
            <div class="col-lg-3 col-md-6">
                <a href="/categories/sports/" class="card category-card text-decoration-none h-100 border-0 shadow-sm">
                    <div class="card-body text-center p-4">
                        <div class="category-icon mb-3">
                            <i class="bi bi-trophy display-4 text-warning"></i>
                        </div>
                        <h5 class="card-title">క్రీడలు</h5>
                        <p class="card-text text-muted small">క్రికెట్, ఇతర క్రీడలు, టూర్నమెంట్ విశేషాలు</p>
                        <span class="badge bg-light text-dark">15 వార్తలు</span>
                    </div>
                </a>
            </div>
        </div>
    </div>
</section>

<!-- Latest News -->
<section class="latest-news py-5 bg-white">
    <div class="container">
        <div class="row mb-4">
            <div class="col">
                <h2 class="fw-bold border-bottom pb-2">తాజా వార్తలు</h2>
            </div>
            <div class="col-auto">
                <a href="/news/" class="btn btn-outline-primary btn-sm">మరిన్ని వార్తలు</a>
            </div>
        </div>
        
        <div class="row g-4">
            {% for post in collections.news %}
            <div class="col-lg-6">
                <div class="card latest-card h-100 border-0 shadow-sm">
                    <div class="row g-0 h-100">
                        {% if post.data.image %}
                        <div class="col-md-4">
                            <img src="{{ post.data.image }}" class="img-fluid rounded-start h-100" alt="{{ post.data.title }}" style="object-fit: cover;">
                        </div>
                        {% endif %}
                        <div class="col-md-{% if post.data.image %}8{% else %}12{% endif %}">
                            <div class="card-body d-flex flex-column h-100">
                                <h6 class="card-title">
                                    <a href="{{ post.url }}" class="text-decoration-none text-dark">{{ post.data.title }}</a>
                                </h6>
                                <p class="card-text text-muted small flex-grow-1">{{ post.data.description | truncate(80) }}</p>
                                <div class="d-flex justify-content-between align-items-center">
                                    <small class="text-muted">
                                        <i class="bi bi-clock me-1"></i>
                                        {{ post.date | readableDate }}
                                    </small>
                                    {% if post.data.categories %}
                                    <span class="badge bg-light text-dark small">{{ post.data.categories[0] }}</span>
                                    {% endif %}
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            {% endfor %}
        </div>
        
        {% if collections.news.length > 6 %}
        <div class="row mt-4">
            <div class="col text-center">
                <button class="btn btn-primary" id="loadMoreBtn">
                    <i class="bi bi-arrow-clockwise me-2"></i>మరిన్ని వార్తలు లోడ్ చేయండి
                </button>
            </div>
        </div>
        {% endif %}
    </div>
</section>

<script>
document.addEventListener('DOMContentLoaded', function() {
    // Load More Button
    const loadMoreBtn = document.getElementById('loadMoreBtn');
    if (loadMoreBtn) {
        loadMoreBtn.addEventListener('click', function() {
            const originalText = this.innerHTML;
            this.innerHTML = '<i class="bi bi-hourglass-split me-2"></i>లోడ్ అవుతోంది...';
            this.disabled = true;
            
            setTimeout(() => {
                this.style.display = 'none';
            }, 1500);
        });
    }
    
    // Newsletter Form
    const newsletterForms = document.querySelectorAll('.newsletter-form');
    newsletterForms.forEach(form => {
        form.addEventListener('submit', function(e) {
            e.preventDefault();
            const email = this.querySelector('input[type="email"]').value;
            const button = this.querySelector('button[type="submit"]');
            
            if (email) {
                const originalText = button.innerHTML;
                button.innerHTML = '<i class="bi bi-hourglass-split me-2"></i>సబ్మిట్ అవుతోంది...';
                button.disabled = true;
                
                setTimeout(() => {
                    button.innerHTML = '<i class="bi bi-check2 me-2"></i>సబ్‌స్క్రైబ్ చేయబడింది!';
                    this.reset();
                    
                    setTimeout(() => {
                        button.innerHTML = originalText;
                        button.disabled = false;
                    }, 2000);
                }, 1000);
            }
        });
    });
    
    // Category card hover effects
    const categoryCards = document.querySelectorAll('.category-card');
    categoryCards.forEach(card => {
        card.addEventListener('mouseenter', function() {
            this.style.transform = 'translateY(-5px)';
            this.style.transition = 'all 0.3s ease';
        });
        
        card.addEventListener('mouseleave', function() {
            this.style.transform = 'translateY(0)';
        });
    });
});
</script>
